{
  "name": "tucker-fresh-catalogue-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "typecheck:functions": "tsc -p tsconfig.functions.json",
    "test:functions": "node --experimental-strip-types --test tests/image-proxy.test.ts",
    "build": "tsc --noEmit && tsc -p tsconfig.functions.json && vite build"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260810.1",
    "typescript": "^5.9.2",
    "vite": "^7.0.6",
    "wrangler": "^4.120.0"
  }
}
